# Copy this file to .env and customize for your local machine (Windows or Linux).

# Host path to SQLite DB file (defaults to the repo DB)
STROPTIMISE_DB=./server/db.sqlite3

# Frontend / proxy settings (used by docker-compose and the frontend image)
# API_URL should be the backend base URL (protocol + host + port), no trailing slash.
# Example: API_URL=http://stroptimise_server:9050
API_URL=http://stroptimise_server:9050

# FRONTEND_PORT is the port nginx listens on inside the frontend container.
# If you keep the nginx template listening on ${FRONTEND_PORT}, map the host port
# using the same value (host:container), e.g. ports: ["${FRONTEND_PORT}:${FRONTEND_PORT}"].
FRONTEND_PORT=9060

# Security / auth (REPLACE BEFORE DEPLOYING)
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET
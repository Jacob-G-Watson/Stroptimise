# Example secrets file for `act` (local only)
# Copy this to `.secrets` and fill in values when running `act` locally.
# Never commit the real `.secrets` file - it's already ignored in .gitignore

DOCKER_USERNAME=
DOCKER_PASSWORD=

# Add other repository secrets below as needed
# EXAMPLE_SECRET=
